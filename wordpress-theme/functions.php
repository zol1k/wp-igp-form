<?php
/**
 * IGP Child Theme — functions.php
 * =================================
 * Loads the IGP form plugin loader which handles all asset enqueueing,
 * AJAX handlers, and shared template helpers.
 */

defined( 'ABSPATH' ) || exit;

require_once get_stylesheet_directory() . '/igp-form/php/loader.php';
